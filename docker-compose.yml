services:
  # truyen-api:
  #   build: 
  #     context: .
  #     dockerfile: Dockerfile
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - NODE_ENV=development
  #     - PORT=3000
  #     - DB_PORT=5678
  #     - DB_HOST=postgres_db
  #   volumes:
  #     - ./src:/usr/src/app/src
  postgres_db:
    image: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ntruyen
    ports:
      - "5678:5432"
  redis_nt:
    image: redis
    ports:
      - "6379:6379"
      